apiVersion: v1
kind: Service
metadata:
  name: {{ include "xorb-api.fullname" . }}
  labels:
    {{- include "xorb-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "xorb-api.selectorLabels" . | nindent 4 }}
